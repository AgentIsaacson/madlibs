import React, { Component } from 'react';

class Card extends Component {
    render() {
        return (
            <div className="card">
                this is where the inputs go
            </div> 
        )
    }
}

export default Card;